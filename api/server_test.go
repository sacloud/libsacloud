package api

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestSearchServer(t *testing.T) {
	assert.True(t, true)

	//client.Region = "is1b"
	//api := client.Server
	//s, err := api.SearchServerByName("sakura-dev")
	//assert.NoError(t, err)
	//assert.NotEmpty(t, s)
}
